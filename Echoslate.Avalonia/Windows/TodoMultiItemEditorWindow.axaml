<UserControl x:Class="Echoslate.Avalonia.Windows.TodoMultiItemEditorWindow"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:behaviors="clr-namespace:Echoslate.Avalonia.Behaviors"
             xmlns:viewModels="clr-namespace:Echoslate.Core.ViewModels;assembly=Echoslate.Core"
             Width="600"
             x:DataType="viewModels:TodoMultiItemEditorViewModel"
             Background="{DynamicResource AppDarkBackgroundBrush}" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Properties  -->
        <Border Grid.Row="0"
                Grid.Column="0"
                Margin="7" >
            <Grid RowDefinitions="Auto,*" >
                <Border Grid.Row="0"
                        Margin="1"
                        Padding="10,0"
                        Background="{DynamicResource ControlBackgroundBrush}"
                        BorderBrush="{DynamicResource BorderSilverBrush}"
                        BorderThickness="1"
                        CornerRadius="3" >
                    <TextBlock FontWeight="SemiBold"
                               FontSize="20"
                               Foreground="{DynamicResource ForegroundBrush}"
                               Text="Properties" />
                </Border>

                <Border Grid.Row="1"
                        Margin="1"
                        Padding="10,0"
                        Background="{DynamicResource ControlBackgroundBrush}"
                        BorderBrush="{DynamicResource BorderSilverBrush}"
                        BorderThickness="1"
                        CornerRadius="3" >
                    <Grid Margin="10,15" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <CheckBox VerticalAlignment="Center"
                                  Classes="toggle-switch"
                                  IsChecked="{Binding IsSeverityChangeable}" />
                        <Button Grid.Column="1"
                                Width="120"
                                Height="40"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Background="{Binding SeverityButtonBackground}"
                                Classes="severity-button"
                                Command="{Binding CycleSeverityCommand}"
                                Content="Severity"
                                FontSize="16"
                                Foreground="{DynamicResource ForegroundBrush}" />

                        <CheckBox Grid.Column="2"
                                  Margin="30,0,0,0"
                                  VerticalAlignment="Center"
                                  Classes="toggle-switch"
                                  Content="Rank:"
                                  FontSize="16"
                                  Foreground="{DynamicResource SubtleForegroundBrush}"
                                  IsChecked="{Binding IsRankChangeable}" />
                        <StackPanel Grid.Column="3"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal" >
                            <Border Height="30"
                                    BorderBrush="{DynamicResource BorderMidBrush}"
                                    BorderThickness="1" >
                                <TextBlock Width="70"
                                           behaviors:MouseWheelNumericBehavior.Enabled="True"
                                           behaviors:MouseWheelNumericBehavior.Minimum="0"
                                           behaviors:MouseWheelNumericBehavior.TargetProperty="Rank"
                                           Background="{DynamicResource TransparentBrush}"
                                           FontSize="16"
                                           Foreground="{DynamicResource ForegroundBrush}"
                                           FontWeight="SemiBold"
                                           LineHeight="30"
                                           IsEnabled="{Binding IsRankEnabled}"
                                           Text="{Binding Rank}"
                                           TextAlignment="Center"
                                           ToolTip.Tip="Mouse wheel: ±1 | Shift+wheel: ±5" />
                            </Border>
                            <Button Width="40"
                                    Height="30"
                                    Padding="0"
                                    Command="{Binding RankToTopCommand}"
                                    Content="▲▲"
                                    IsEnabled="{Binding IsRankEnabled}"
                                    FontSize="16"
                                    Foreground="{DynamicResource ForegroundBrush}"
                                    ToolTip.Tip="Move to top" />
                            <Button Width="40"
                                    Height="30"
                                    Padding="0"
                                    Command="{Binding RankToBottomCommand}"
                                    Content="▼▼"
                                    IsEnabled="{Binding IsRankEnabled}"
                                    FontSize="16"
                                    Foreground="{DynamicResource ForegroundBrush}"
                                    ToolTip.Tip="Move to bottom" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!--  TO DO Text  -->
        <Border Grid.Row="1"
                Margin="7"
                Background="{DynamicResource ControlBackgroundBrush}" >
            <Grid RowDefinitions="Auto,*" >
                <Border Grid.Row="0"
                        Margin="1"
                        Padding="10,0"
                        Background="{DynamicResource ControlBackgroundBrush}"
                        BorderBrush="{DynamicResource BorderSilverBrush}"
                        BorderThickness="1"
                        CornerRadius="3" >
                    <TextBlock FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource ForegroundBrush}"
                               Text="Title" />
                </Border>
                <Border Grid.Row="1"
                        Margin="1"
                        Padding="10,0"
                        Background="{DynamicResource ControlBackgroundBrush}"
                        BorderBrush="{DynamicResource BorderSilverBrush}"
                        BorderThickness="1"
                        CornerRadius="3" >
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <CheckBox Margin="10,0,0,0"
                                  VerticalAlignment="Center"
                                  Classes="toggle-switch"
                                  FontSize="16"
                                  Foreground="{DynamicResource SubtleForegroundBrush}"
                                  IsChecked="{Binding IsTodoChangeable}" />
                        <TextBox Grid.Column="1"
                                 Height="60"
                                 Margin="20,0,0,0"
                                 AcceptsReturn="True"
                                 FontSize="16"
                                 Foreground="{DynamicResource ForegroundBrush}"
                                 Text="{Binding Todo, UpdateSourceTrigger=PropertyChanged}"
                                 TextWrapping="Wrap" />
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!--  Tags  -->
        <Border Grid.Row="2"
                Margin="7" >
            <Grid RowDefinitions="Auto,*" >
                <Border Grid.Row="0"
                        Margin="1"
                        Padding="10,0"
                        Background="{DynamicResource ControlBackgroundBrush}"
                        BorderBrush="{DynamicResource BorderSilverBrush}"
                        BorderThickness="1"
                        CornerRadius="3" >
                    <TextBlock FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource ForegroundBrush}"
                               Text="Tags" />
                </Border>
                <Border Grid.Row="1"
                        Margin="1"
                        Padding="10,0"
                        Background="{DynamicResource ControlBackgroundBrush}"
                        BorderBrush="{DynamicResource BorderSilverBrush}"
                        BorderThickness="1"
                        CornerRadius="3" >
                    <Grid Margin="10,15" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Top" >
                            <Button Height="35"
                                    Margin="3"
                                    VerticalAlignment="Top"
                                    Command="{Binding AddTagCommand}"
                                    FontSize="16"
                                    Foreground="{DynamicResource ForegroundBrush}"
                                    Content="Add Tag" />
                            <CheckBox Margin="0,10,0,8"
                                      HorizontalAlignment="Left"
                                      Classes="toggle-switch"
                                      FontSize="16"
                                      Foreground="{DynamicResource SubtleForegroundBrush}"
                                      IsChecked="{Binding IsTagChangeable}" />
                        </StackPanel>

                        <ListBox Grid.Column="1"
                                 Height="150"
                                 Padding="7,0"
                                 Margin="0"
                                 ItemsSource="{Binding Tags}" >
                            <ListBox.ItemTemplate>
                                <DataTemplate x:CompileBindings="False" >
                                    <Grid Height="35"
                                          Margin="7,0" >
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Button Background="{DynamicResource DangerRedBrush}"
                                                Command="{Binding $parent[UserControl].DataContext.DeleteTagCommand}"
                                                CommandParameter="{Binding}"
                                                Content="X"
                                                Padding="0"
                                                Height="24"
                                                FontSize="16"
                                                Foreground="{DynamicResource ForegroundBrush}"
                                                FontWeight="Bold" />
                                        <Label Grid.Column="1"
                                               Padding="7,0"
                                               VerticalContentAlignment="Center"
                                               MinWidth="140"
                                               Margin="8,0,0,0"
                                               Background="{DynamicResource ButtonBackgroundBrush}"
                                               BorderThickness="0,0,0,1"
                                               FontSize="16"
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               Content="{Binding}" />
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!--  Bottom Buttons  -->
        <StackPanel Grid.Row="3"
                    Margin="0,10,0,10"
                    HorizontalAlignment="Center"
                    Orientation="Horizontal" >
            <CheckBox Margin="0,0,3,0"
                      VerticalAlignment="Center"
                      Classes="toggle-switch"
                      FontSize="16"
                      Foreground="{DynamicResource SubtleForegroundBrush}"
                      IsChecked="{Binding IsCompleteChangeable}" />

            <Button Classes="complete"
                    Click="Complete_OnClick"
                    IsEnabled="{Binding IsCompleteChangeable}" />
            <Button Classes="ok"
                    Click="Ok_OnClick"
                    IsDefault="True" />
            <Button Classes="cancel"
                    Click="Cancel_OnClick"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</UserControl>