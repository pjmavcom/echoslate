<Window x:Class="Echoslate.DlgTodoMultiItemEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="Todo Multi Item Edit"
        Width="610" Height="676"
        Style="{StaticResource {x:Type Window}}"
        mc:Ignorable="d"
        >
    <Window.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding CompleteCommand}" />
    </Window.InputBindings>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Row 0: Severity + Rank  -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="60" />
                <ColumnDefinition Width="30" />
                <ColumnDefinition Width="30" />
                <ColumnDefinition Width="60" />
                <ColumnDefinition Width="60" />
            </Grid.ColumnDefinitions>

            <CheckBox x:Name="ckSev"
                      Grid.Column="0"
                      Margin="2" VerticalAlignment="Center"
                      IsChecked="{Binding IsSeverityEnabled, Mode=TwoWay}"
                      />
            <Button Grid.Column="1"
                    Width="Auto"
                    Margin="4,0" Padding="16,8"
                    Background="{Binding SeverityButtonBackground}"
                    BorderThickness="0"
                    Command="{Binding CycleSeverityCommand}"
                    Content="Severity" FontWeight="SemiBold" Foreground="White"
                    />

            <CheckBox x:Name="ckRank"
                      Grid.Column="3"
                      Margin="2,2,10,2" VerticalAlignment="Center"
                      Content="Rank:"
                      IsChecked="{Binding IsRankEnabled, Mode=TwoWay}"
                      />
            <xctk:IntegerUpDown Grid.Column="4"
                                Width="60"
                                Margin="2"
                                Increment="1" Minimum="0"
                                Value="{Binding Rank, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                />

            <Button Grid.Column="5"
                    Command="{Binding RankToBottomCommand}"
                    Content="▼▼" FontSize="16"
                    />
            <Button Grid.Column="6"
                    Command="{Binding RankToTopCommand}"
                    Content="▲▲" FontSize="16"
                    />
        </Grid>

        <!--  Row 1: Todo text  -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="500" />
            </Grid.ColumnDefinitions>

            <CheckBox Grid.Column="0"
                      Margin="2" VerticalAlignment="Top"
                      Content="TODO:"
                      IsChecked="{Binding IsTodoEnabled, Mode=TwoWay}"
                      />
            <TextBox Grid.Column="1"
                     Height="200"
                     Margin="2"
                     AcceptsReturn="True"
                     Text="{Binding Todo}"
                     TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                     />
        </Grid>

        <!--  Row 2 & 3: Tags section  -->
        <Grid Grid.Row="2" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  Left column: Tags checkbox + Add button  -->
            <StackPanel Grid.Column="0" VerticalAlignment="Top">
                <CheckBox x:Name="ckTags"
                          Margin="2" HorizontalAlignment="Right"
                          Content="Tags:"
                          IsChecked="{Binding IsTagEnabled, Mode=TwoWay}"
                          />
                <Button Width="72" Height="35"
                        Margin="2,5,2,0" HorizontalAlignment="Right"
                        Command="{Binding AddTagCommand}"
                        Content="Add Tag"
                        />
            </StackPanel>

            <!--  Right column: ListBox  -->
            <ListBox x:Name="lbTags"
                     Grid.Column="1"
                     Margin="2"
                     ItemsSource="{Binding Tags}"
                     >
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Width="490" Height="34">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0"
                                    Width="30" Height="30"
                                    Command="{Binding DataContext.DeleteTagCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}"
                                    Content="X"
                                    />
                            <TextBox Grid.Column="1"
                                     Margin="5,0"
                                     Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <!--  Row 4: Bottom buttons  -->
        <StackPanel Grid.Row="4"
                    Margin="0,15,0,0" HorizontalAlignment="Center"
                    Orientation="Horizontal"
                    >
            <CheckBox x:Name="ckComplete"
                      Margin="0,0,20,0" VerticalAlignment="Center"
                      IsChecked="{Binding IsCompleteEnabled, Mode=TwoWay}"
                      />
            <Button Width="150" Height="35"
                    Margin="0,0,10,0"
                    Command="{Binding CompleteCommand}"
                    Content="{Binding CompleteButtonContent}"
                    IsEnabled="{Binding IsChecked, ElementName=ckComplete}"
                    />
            <Button Width="150" Height="35"
                    Margin="0,0,10,0"
                    Command="{Binding OkCommand}"
                    Content="OK" IsDefault="True"
                    />
            <Button Width="150" Height="35"
                    Command="{Binding CancelCommand}"
                    Content="Cancel" IsCancel="True"
                    />
        </StackPanel>
    </Grid>
</Window>