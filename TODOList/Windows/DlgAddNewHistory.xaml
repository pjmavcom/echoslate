<Window x:Class="Echoslate.DlgAddNewHistory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="Add New History Item"
        Width="400" Height="100"
        FocusManager.FocusedElement="{Binding ElementName=tbTitle}"
        Style="{StaticResource {x:Type Window}}"
        mc:Ignorable="d"
        >
    <Grid>
        <StackPanel>
            <TextBox Name="tbTitle" />
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <Button Width="150" Height="35"
                        Click="Ok_OnClick" Content="Ok" IsDefault="True" KeyboardNavigation.IsTabStop="False"
                        />
                <Button Width="150" Height="35"
                        Click="Cancel_OnClick" Content="Cancel" IsCancel="True" KeyboardNavigation.IsTabStop="False"
                        />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
